def leer_prestamos():
  prestamos = []

#La apertura 'a+' en Python abre un fichero para añadir (append) contenido al final y para leer (read) su contenido, sin borrar lo existente.
  with open("prestamos.txt", "a+", en)